# 🎯 Victorinka — Мультиплеерная платформа викторин

Добро пожаловать в **Victorinka** — современную платформу для создания и проведения мультиплеерных викторин в режиме реального времени. Проект реализован на современном технологическом стеке: **Django + DRF** (бэкенд), **React** (фронтенд), **PostgreSQL**, **WebSocket**, **RabbitMQ**, **Redis**, и всё это упаковано в **Docker Compose** для быстрого запуска.

---

## 🚀 Быстрый старт

### Требования

Убедитесь, что у вас установлены:
- **Docker** и **Docker Compose** (версия 2.0+)
- **Git**

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/victorinka.git
   cd victorinka1
   ```

2. **Настройте переменные окружения:**
   ```bash
   copy .env.example .env
   ```
   Отредактируйте `.env` файл при необходимости (пароли БД, секретные ключи и т.д.)

3. **Запустите проект одной командой:**
   ```bash
   docker-compose up --build
   ```

4. **Откройте в браузере:**
   - 🏠 **Главная страница:** [http://localhost](http://localhost)
   - 🔑 **Админ-панель Django:** [http://localhost/admin](http://localhost/admin)  
     *(Логин: `admin@victorinka.com` / Пароль: из `.env`)*
   - 📚 **Swagger API:** [http://localhost/swagger](http://localhost/swagger)
   - 📖 **ReDoc API:** [http://localhost/redoc](http://localhost/redoc)

> ⚠️ **Примечание:** При первом запуске может потребоваться 1–2 минуты на инициализацию баз данных и миграций.

---

## 🧩 Архитектура проекта

```
victorinka1/
├── backend/                    # Django + DRF бэкенд
│   ├── src/
│   │   ├── apps/               # Приложения Django
│   │   │   ├── users/          # Пользователи, аутентификация, закладки, история
│   │   │   ├── rooms/          # Игровые комнаты, приглашения
│   │   │   ├── questions/      # Викторины, вопросы, темы, теги
│   │   │   ├── game/           # Игровые сессии, раунды, ответы
│   │   │   └── core/           # Базовые view (home, health)
│   │   ├── config/             # Настройки Django, URLs, ASGI/WSGI
│   │   ├── manage.py
│   │   └── run_dev.py
│   └── requirements.txt
│
├── frontend/                   # React фронтенд
│   ├── src/
│   │   ├── components/         # React компоненты (Header, Footer)
│   │   ├── pages/              # Страницы (Home, Login, Register)
│   │   ├── services/           # API клиенты (axios, authAPI)
│   │   └── styles/             # CSS модули
│   ├── public/
│   └── package.json
│
├── docker/                     # Docker конфигурация
│   ├── backend/Dockerfile
│   ├── frontend/Dockerfile
│   └── nginx/nginx.dev.conf
│
├── docker-compose.yml          
├── .env.example                # Шаблон переменных окружения
└── README.md
```

---

## 🛠️ Основные технологии

### Backend
- **Python 3.13** — язык программирования
- **Django 5.2** — веб-фреймворк
- **Django REST Framework (DRF)** — REST API
- **djangorestframework-simplejwt** — JWT аутентификация
- **drf-yasg** — автогенерация Swagger/OpenAPI документации
- **PostgreSQL** — основная реляционная БД
- **psycopg2-binary** — PostgreSQL адаптер
- **pytest** — тестирование

### Frontend
- **React 19** — UI библиотека
- **React Router DOM** — маршрутизация
- **Axios** — HTTP клиент
- **Ant Design** — UI компоненты

### Инфраструктура
- **Docker** — контейнеризация
- **Nginx** — реверс-прокси (API → backend, остальное → frontend)
- **PostgreSQL 16** — база данных

### Планируется к интеграции
- **RabbitMQ** — брокер сообщений для фоновых задач
- **Celery** — асинхронные задачи
- **Redis** — кэширование и WebSocket сессии
- **Django Channels** — WebSocket для real-time игры

---

## 🎮 Игровая механика (текущий статус)

### Реализовано ✅

1. **Аутентификация и профиль пользователя:**
   - Регистрация и вход по email/паролю (JWT токены)
   - Управление профилем (никнейм, email)
   - Выход из системы (с инвалидацией refresh-токенов)
   - История игр и статистика (очки, победы)

2. **Управление викторинами:**
   - Создание викторин с названием, описанием, тегами и темами
   - CRUD операции над вопросами и вариантами ответов
   - Публичные и приватные викторины
   - Статусы: черновик, опубликована, архив

3. **Комнаты и приглашения:**
   - Создание игровых комнат с уникальным кодом приглашения
   - Присоединение к комнате по коду
   - Роли: хост и игрок
   - Статусы комнат: черновик, открыта, идёт игра, завершена

4. **Игровые сессии:**
   - Привязка викторины к комнате
   - Отслеживание прогресса (текущий вопрос, статус)
   - Раунды игры с таймерами
   - Сохранение ответов игроков
   - Подсчёт очков и финальных результатов

5. **API эндпоинты:**
   - `/api/auth/` — аутентификация (register, login, logout, me)
   - `/api/users/` — профиль, закладки, история, статистика
   - `/api/rooms/` — комнаты (создание, вход, выход, список)
   - `/api/questions/` — викторины и вопросы (CRUD, каталог, поиск)
   - `/api/game/` — игровые сессии, раунды, ответы
   - `/swagger/` — интерактивная документация API

### В разработке 🚧

- **WebSocket интеграция** для синхронизации игроков в реальном времени
- **Таблица лидеров** (глобальная и по викторинам)
- **Система закладок** викторин
- **Фильтры и сортировка** каталога викторин
- **Модерация контента** через админ-панель

---

## 📡 API Примеры

### Регистрация нового пользователя
```bash
curl -X POST http://localhost/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "nickname": "player1",
    "password": "SecurePass123"
  }'
```

### Вход в систему (получение токенов)
```bash
curl -X POST http://localhost/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123"
  }'
```

### Создание комнаты
```bash
curl -X POST http://localhost/api/rooms/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Моя первая викторина"
  }'

# Ответ:
# {
#   "id": 1,
#   "name": "Моя первая викторина",
#   "invite_code": "D39D7Q",
#   "status": "draft",
#   ...
# }
```

### Присоединение к комнате по коду
```bash
curl -X POST http://localhost/api/rooms/join/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "invite_code": "D39D7Q"
  }'
```

> Больше примеров и полная документация доступны в **Swagger UI**: [http://localhost/swagger](http://localhost/swagger)


---

## 🔧 Полезные команды

### Docker

```bash
# Запуск без пересборки
docker-compose up

# Пересоздать образы (после изменения requirements.txt или package.json)
docker-compose build --no-cache
docker-compose up

# Остановка всех контейнеров
docker-compose down

# Удалить контейнеры и volumes (база данных будет удалена!)
docker-compose down -v
```

### Django

```bash
# Создать миграции
docker-compose exec backend python manage.py makemigrations

# Применить миграции
docker-compose exec backend python manage.py migrate

# Создать суперпользователя
docker-compose exec backend python manage.py createsuperuser

# Собрать статику
docker-compose exec backend python manage.py collectstatic --noinput

# Открыть Django shell
docker-compose exec backend python manage.py shell
```

### База данных

```bash
# Подключиться к PostgreSQL
docker-compose exec db psql -U victorinka_user -d victorinka_db

# Бэкап базы данных
docker-compose exec db pg_dump -U victorinka_user victorinka_db > backup.sql

# Восстановление из бэкапа
docker-compose exec -T db psql -U victorinka_user victorinka_db < backup.sql
```

### Frontend

```bash
# Установить зависимости
docker-compose exec frontend npm install

# Собрать production build
docker-compose exec frontend npm run build
```

---

## 📁 Модели данных

### User (Пользователь)
- `email` — уникальный email (используется для входа)
- `nickname` — отображаемое имя игрока
- `total_points` — общее количество очков
- `total_wins` — количество побед
- `is_email_verified` — подтверждение email

### Quiz (Викторина)
- `title` — название викторины
- `description` — описание
- `author` — создатель викторины
- `topics` — темы (ManyToMany)
- `tags` — теги (ManyToMany)
- `visibility` — публичная/приватная
- `status` — черновик/опубликована/архив

### Question (Вопрос)
- `quiz` — привязка к викторине
- `text` — текст вопроса
- `question_type` — тип (single_choice, multiple_choice, true_false)
- `time_limit` — время на ответ (секунды)
- `points` — очки за правильный ответ

### Room (Комната)
- `name` — название комнаты
- `host` — хост (создатель)
- `invite_code` — уникальный код приглашения (6 символов)
- `status` — статус (черновик, открыта, идёт игра, завершена)

### GameSession (Игровая сессия)
- `room` — комната
- `quiz` — викторина
- `status` — статус (ожидание, идёт игра, пауза, завершена)
- `current_question_index` — индекс текущего вопроса
- `started_at`, `finished_at` — временные метки

---
**Victorinka** — викторины нового поколения! 🎯🏆

